PORT=8787

# Zoho EU (falls EU-DC; sonst .com)
IMAP_HOST=imap.zoho.eu
IMAP_PORT=993
IMAP_USER=inbox@sigmacode.ai
IMAP_PASS=w7Fwi2dukJA2

IMAP_TLS=true

# Dev
DEV_MODE=true

# Frontend (Vite auf 3092)
CORS_ORIGINS=http://localhost:3092

# Admin-Token für Dev-Endpoints
ADMIN_DEV_TOKEN=dev-local-admin

# Scheduler: Intervall in Sekunden (z. B. 900 = 15 Minuten)
IMAP_FETCH_INTERVAL_SEC=900
